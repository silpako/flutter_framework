import 'package:flutter/material.dart';

class Favourites_ecmrce extends StatelessWidget {
  const Favourites_ecmrce({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
